# Copy this file to .env and fill in your values.
# Never commit .env to version control.

# Base URL for API calls.
# During local dev (npm run dev) the Vite proxy rewrites /api → http://localhost:8000
# so you can leave this blank. Set it when building a Docker image.
VITE_API_URL=http://localhost:8000
